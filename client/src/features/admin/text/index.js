/**
 * Admin text (and the bilingual login slip). To add a full Bangla UI later, create bn.js with
 * the same shape and pick it here; components import `text` and never change.
 */
import en from './en.js';

export const text = en;
