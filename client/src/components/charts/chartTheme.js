/** Hex values of the index.css tokens, for SVG attributes Recharts sets directly. */
export const CHART_COLORS = Object.freeze({
  line: '#1e3309',
  area: '#e9f0da',
  good: '#849a28',
  low: '#e23260',
  threshold: '#b0173f',
  grid: '#f1e1dc',
  axis: '#5f5a52',
});

export const AXIS_TICK = Object.freeze({ fill: CHART_COLORS.axis, fontSize: 13 });
